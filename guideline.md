Context:

- This project is built for multi utilities creation focusing on Chrome Extension
- Each extension should be stored in one specific folder (created by user or by claude)

Generate steps:

- Step 1: user provide the context and the need of the utility
- Step 2: create and stored the utility in the right extension
- Step 3: create icon for each utility
- Step 4: create a suitable banner 1200 * 800 for social media

Note:
- All Utilities must be safe (no library that can leak user data)
- Each time we update the utility we should push to GitHub immediately